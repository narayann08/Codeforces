#include <iostream>
using namespace std;

int main(){
    long long int a,b;

    cin >> a >> b;
    
    cout << (a % 10) + (b % 10) << endl;

    // int x = a[a.size() - 1] - '0';
    // int y = b[b.size() - 1] - '0';

    return 0;
}
